# Simple Client

A minimal client that tests the WebSocket policy server. 

## Usage

Terminal window 1:
```bash
uv run python scripts/eval.py
```

Terminal window 2:
```bash
uv run python clients/simple_client/main.py
```

